name: Runtime coverage deployment probe

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  explain:
    runs-on: ubuntu-latest
    steps:
      - name: Record disabled retry loop
        run: echo "Automatic marker commits are disabled; deployment state is verified by production smoke checks."
